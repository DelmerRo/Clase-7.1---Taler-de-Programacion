// import checkpoint from "../models/checkpoint"

export class CheckpointUseCase {
  constructor(persona) {
    this.persona = persona
  }

  mark(identificador) {
    // if identificador == valid (persona y checkpoint)

    // checkpoint = new Checkpoint(...)

    // CheckpointRepository.save(checkpoint)

    return checkpoint
  }
}
